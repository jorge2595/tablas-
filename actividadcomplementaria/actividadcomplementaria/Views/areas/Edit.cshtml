@model actividadcomplementaria.Models.area

@{
    ViewData["Title"] = "Edit";
}

<h2>Editar</h2>

<h4>area</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ID_area" />
            <div class="form-group">
                <label asp-for="Nombre_area" class="control-label"></label>
                <input asp-for="Nombre_area" class="form-control" />
                <span asp-validation-for="Nombre_area" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Regresar</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
